import { SafeAreaView } from "react-native";
import Search from "../components/Search";

const Home = () => {
  return (
    <SafeAreaView>
      <Search />
    </SafeAreaView>
  );
};

export default Home;
